// API: GET /api/profile — return logged-in user's profile data
// API: PUT /api/profile — update name or password (hash new password with bcryptjs)
// Read JWT from cookie to identify the user
// Return 401 if not authenticated

export default async function handler(req, res) {}
