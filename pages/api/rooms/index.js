// API: GET /api/rooms — return list of all active rooms
// Supports query params: ?search=name&capacity=10&type=lab
// Reads from "rooms" table
// Returns: [{ id, name, capacity, location, type, status }, ...]

export default async function handler(req, res) {}
