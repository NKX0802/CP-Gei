// API: GET /api/rooms/[id] — return full details of one room
// Reads from "rooms" table by id
// Returns: { id, name, capacity, location, type, facilities, description, status }
// Return 404 if room not found

export default async function handler(req, res) {}
